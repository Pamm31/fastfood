import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;

  input {
    background: #f4f4f4;
    border: none;
  }

  input::placeholder {
    font-weight: bold;
    opacity: 60%;
  }
`;
